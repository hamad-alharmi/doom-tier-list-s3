# Copy to .env.local for `vercel dev` — or just set these in the Vercel dashboard.
ADMIN_PASSWORD=changeme
# These two are auto-filled when you connect the Redis database in Vercel:
# KV_REST_API_URL=
# KV_REST_API_TOKEN=